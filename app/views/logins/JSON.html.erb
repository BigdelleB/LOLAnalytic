class DATA

require 'net/http'



	def extractJSON(login)
		username= login.Username
		region= login.Region
		url="https://"+region+"pvp.net/api/lol/"+region+"/v1.4/summoner/by-name/" + username+"?api_key=28ba1d65-cda8-4e79-90ad-	aad6b1ab6326"
		url=URI.parse(url)

		result= Net::HTTP.get(URI.parse(url))
		puts result;
	end
end

	